enum RoutesEnum {
  MAIN = '#main',
  LOGIN = '#login',
  REGISTRATION = '#registration',
  MAIN_MENU = '#main-menu',
  JOIN_LOBBY = '#join-lobby',
  LOBBY = '#lobby',
  GAME = '#game',
}

export default RoutesEnum;
